## Packages
framer-motion | Page transitions, scroll-triggered animations, and fluid section reveals
react-hook-form | Form state management for the contact form
@hookform/resolvers | Zod validation resolver for the contact form

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

Background context:
This is a completely static portfolio meant for AWS S3 hosting. 
It requires NO backend connection. The contact form mutation resolves instantly with a success toast.
